version: '2'
services:
  web:
    image: markwatsonatx/node-alpine:latest    
    volumes:
      - ./src/app:/usr/src/app
    working_dir: /usr/src/app
    command: npm start
    ports:
      - "33000:3000"
